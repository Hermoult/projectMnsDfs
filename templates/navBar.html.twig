<nav class="navbar">
	<a href="{{ path('listing') }}">Accueil</a>
	<a href="{{ path('seance_index') }}">Seances</a>
	<a href="{{ path('create_film') }}">Create film</a>
	<a href="{{ path('seance_new') }}">Seance film</a>
</nav>
