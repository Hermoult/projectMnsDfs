{% extends 'base.html.twig' %}

{% block title %}Edit Seance
{% endblock %}

{% block body %}
	<h1>Edit Seance</h1>

	{{ include('seance/_form.html.twig', {'button_label': 'Update'}) }}
	<div class="textAlign_center">
		<a href="{{ path('seance_index') }}">back to list</a>
	</div>

	{{ include('seance/_delete_form.html.twig') }}
{% endblock %}
